# assets/

This folder holds static images used by the personal website.

Organization applied:
- `assets/social/`  — social icons (LinkedIn, GitHub, etc.)
- `assets/logos/`   — larger brand or project logos (empty for now)
- `assets/other/`   — miscellaneous images that don't fit above

Notes:
- Some files retain Windows Zone.Identifier ADS metadata; don't worry about those entries.
- I moved `github.png` into `assets/social/` and moved `nhs.png` and `UIL.png` into `assets/other/`.

If you want me to move additional images into `logos/` or rename any files, tell me which ones and I'll update them.
